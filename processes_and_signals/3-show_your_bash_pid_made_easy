#!/usr/bin/env bash
#using top
# shellcheck disable=SC2009
pids=$(pgrep bash)
name=$(top -o cmd | findstr bash)
for pid in $pids; do
	process_name=$( "$name" | tr '\0' ' ')
	echo "PID: $pid, Process Name: $process_name"
	
done
