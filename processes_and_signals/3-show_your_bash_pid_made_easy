#!/usr/bin/env bash
#using top
# shellcheck disable=SC2009
pids=$(pgrep bash)
for pid in $pids; do
	process_name=$( /proc/"$pid"/cmdline | tr '\0' ' ')
	echo "PID: $pid, Process Name: $process_name"
	
done
