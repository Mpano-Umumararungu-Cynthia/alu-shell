#!/usr/bin/env bash
#using top
# shellcheck disable=SC2009
top_output=$(top -b -n 1 | grep "bash")
while read -r line;
do
	pid=$(echo "$line" | awk '{print $1}')
	process_name=$(echo "$line" | awk '{print $12}')
	echo "hey"
	echo "PID: $pid, Process Name: $process_name"
	
done <<< "$top_output"
